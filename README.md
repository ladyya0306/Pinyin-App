# 趣味拼音

本地运行此教育应用的指南。

## 本地运行

**先决条件：** Node.js

1. 安装依赖:
   `npm install`
2. 配置环境变量:
   在 `.env.local` 文件中设置你的 `ALIYUN_API_KEY` (阿里云百炼 API Key)。
3. 运行应用:
   `npm run dev`
   (或在 Windows 系统双击 `start.bat`)
